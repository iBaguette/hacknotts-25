import pygame, os, math

from modules.utilities import *

class GUI:

    def __init__(self, screen):
        
        # This function is called once at the start of the program
        # Use this to calcualte the positions of things
        #                       load images, etc...


        # Maybe variables for like
        #self.health = 100
        #self.coins = 0
        #self.coinsImage = pygame.image.load(os.path.load("assets", "gui")))
        pass

    def draw(self, screen):

        #screen.blit(self.coinsImage, (100, 100))
        #screen.get_width()
        #screen.get_height()
        pass
        ## This function is called constantly! Use it to draw the GUI
        
    